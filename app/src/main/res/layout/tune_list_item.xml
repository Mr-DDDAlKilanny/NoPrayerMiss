<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.appcompat.widget.AppCompatRadioButton
        android:id="@+id/radioSelected"
        android:layout_centerVertical="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/txtTuneName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/radioSelected"
        android:layout_toEndOf="@id/radioSelected"
        android:layout_toLeftOf="@id/btnPreviewTune"
        android:layout_toStartOf="@id/btnPreviewTune"
        android:gravity="start"
        android:textSize="16sp"
        android:textStyle="bold"
        android:text="Test" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:id="@+id/btnPreviewTune"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@android:color/white"
        app:srcCompat="@drawable/baseline_play_circle_outline_24" />
</RelativeLayout>